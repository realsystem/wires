services:
  # Production build
  wire-calculator:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wire-calculator
    ports:
      - "8080:80"
    restart: unless-stopped
    networks:
      - wire-network

  # Development mode with hot reload
  wire-calculator-dev:
    image: node:20-alpine
    container_name: wire-calculator-dev
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "3000:3000"
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
    environment:
      - NODE_ENV=development
    networks:
      - wire-network
    profiles:
      - dev

networks:
  wire-network:
    driver: bridge
